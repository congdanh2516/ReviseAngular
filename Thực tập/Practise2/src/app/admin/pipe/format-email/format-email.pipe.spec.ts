import { FormatEmailPipe } from './format-email.pipe';

describe('FormatEmailPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatEmailPipe();
    expect(pipe).toBeTruthy();
  });
});
